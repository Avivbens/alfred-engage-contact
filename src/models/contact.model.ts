export interface IContact {
    identifier: string
    firstName: string
    lastName: string
    nickname: string
    birthday: string
    phoneNumbers: string[]
    emailAddresses: string[]
    postalAddresses: string[]
}
